---
import MainLayout from '../layouts/MainLayout.astro';
import { getUpcomingMatches, getRecentMatches } from '../services/footballApiService';

// Obtener tanto partidos próximos como recientes
let upcomingMatches = [];
let recentMatches = [];
let error = null;

try {
  // Cargar datos en paralelo para mejor rendimiento
  const [upcomingData, recentData] = await Promise.all([
    getUpcomingMatches(5),
    getRecentMatches(3)
  ]);
  
  upcomingMatches = upcomingData.response || [];
  recentMatches = recentData.response || [];
  
  console.log(`✅ Cargados ${upcomingMatches.length} partidos próximos y ${recentMatches.length} recientes`);
} catch (e) {
  console.error("Error al cargar los partidos:", e);
  error = "No se pudieron cargar los datos de partidos";
}
---

<MainLayout title="FútbolStats - Estadísticas y predicciones deportivas">
  <main>
    <section class="hero">
      <div class="container">
        <h1>FútbolStats</h1>
        <p>Tu portal de estadísticas y predicciones deportivas</p>
        <a href="/equipos" class="cta-button">Explorar Equipos</a>
      </div>
    </section>

    {error && <div class="error-banner">{error}</div>}
  
    <section class="featured-matches">
      <div class="container">
        <h2>Próximos partidos destacados</h2>
        
        <div class="matches-grid">
          {upcomingMatches.length === 0 && !error ? (
            <p class="loading-message">Cargando próximos partidos...</p>
          ) : (
            upcomingMatches.map(match => (
              <div class="match-card">
                <div class="match-date">
                  {new Date(match.fixture.date).toLocaleDateString('es-ES', {
                    weekday: 'short',
                    day: '2-digit',
                    month: 'short',
                    hour: '2-digit',
                    minute: '2-digit'
                  })}
                </div>
                <div class="match-teams">
                  <div class="team home-team">
                    <img 
                      src={match.teams.home.logo} 
                      alt={match.teams.home.name}
                      width="30"
                      height="30"
                      loading="lazy"
                      onerror="this.src='/assets/team-placeholder.png';"
                    />
                    <span>{match.teams.home.name}</span>
                  </div>
                  <div class="match-vs">VS</div>
                  <div class="team away-team">
                    <img 
                      src={match.teams.away.logo} 
                      alt={match.teams.away.name}
                      width="30"
                      height="30"
                      loading="lazy"
                      onerror="this.src='/assets/team-placeholder.png';"
                    />
                    <span>{match.teams.away.name}</span>
                  </div>
                </div>
                <div class="match-league">
                  <span>{match.league.name}</span>
                </div>
                <a href={`/partidos/${match.fixture.id}`} class="match-details-link">
                  Ver detalles y predicciones
                </a>
              </div>
            ))
          )}
        </div>
      </div>
    </section>
    
    <section class="recent-matches">
      <div class="container">
        <h2>Resultados recientes</h2>
        
        <div class="matches-grid">
          {recentMatches.length === 0 && !error ? (
            <p class="loading-message">Cargando resultados recientes...</p>
          ) : (
            recentMatches.map(match => (
              <div class="match-card result-card">
                <div class="match-date">
                  {new Date(match.fixture.date).toLocaleDateString('es-ES', {
                    weekday: 'short',
                    day: '2-digit',
                    month: 'short'
                  })}
                </div>
                <div class="match-teams">
                  <div class="team home-team">
                    <img 
                      src={match.teams.home.logo} 
                      alt={match.teams.home.name}
                      width="30"
                      height="30"
                      loading="lazy"
                      onerror="this.src='/assets/team-placeholder.png';"
                    />
                    <span>{match.teams.home.name}</span>
                  </div>
                  <div class="match-result">
                    <span>{match.goals.home} - {match.goals.away}</span>
                  </div>
                  <div class="team away-team">
                    <img 
                      src={match.teams.away.logo} 
                      alt={match.teams.away.name}
                      width="30"
                      height="30"
                      loading="lazy"
                      onerror="this.src='/assets/team-placeholder.png';"
                    />
                    <span>{match.teams.away.name}</span>
                  </div>
                </div>
                <div class="match-league">
                  <span>{match.league.name}</span>
                </div>
                <a href={`/partidos/${match.fixture.id}`} class="match-details-link">
                  Ver resumen y estadísticas
                </a>
              </div>
            ))
          )}
        </div>
        <div class="view-more">
          <a href="/partidos" class="view-more-link">Ver todos los partidos</a>
        </div>
      </div>
    </section>

    <section class="features">
      <div class="container">
        <h2>Explora nuestras funcionalidades</h2>
        <div class="features-grid">
          <div class="feature-card">
            <div class="feature-icon">📊</div>
            <h3>Estadísticas de jugadores</h3>
            <p>Analiza el rendimiento de tus jugadores favoritos con datos detallados sobre goles, asistencias y más.</p>
            <a href="/jugadores" class="feature-link">Ver jugadores</a>
          </div>
          <div class="feature-card">
            <div class="feature-icon">⚽</div>
            <h3>Equipos en detalle</h3>
            <p>Toda la información sobre los equipos: formaciones, resultados históricos y rendimiento actual.</p>
            <a href="/equipos" class="feature-link">Ver equipos</a>
          </div>
          <div class="feature-card">
            <div class="feature-icon">🔮</div>
            <h3>Predicciones de partidos</h3>
            <p>Descubre nuestras predicciones basadas en análisis estadísticos avanzados y tendencias históricas.</p>
            <a href="/predicciones" class="feature-link">Ver predicciones</a>
          </div>
        </div>
      </div>
    </section>
  </main>
</MainLayout>

<style>
  /* Hero section */
  .hero {
    background: linear-gradient(135deg, #1a2a6c, #b21f1f, #fdbb2d);
    color: white;
    padding: 4rem 0;
    text-align: center;
  }
  
  .hero h1 {
    font-size: 3rem;
    margin-bottom: 1rem;
  }
  
  .hero p {
    font-size: 1.25rem;
    margin-bottom: 2rem;
    opacity: 0.9;
  }
  
  .cta-button {
    background-color: white;
    color: #1a2a6c;
    padding: 0.75rem 1.5rem;
    border-radius: 30px;
    font-weight: bold;
    text-decoration: none;
    transition: all 0.3s ease;
    display: inline-block;
  }
  
  .cta-button:hover {
    transform: translateY(-3px);
    box-shadow: 0 10px 20px rgba(0,0,0,0.2);
  }
  
  /* Sections */
  section {
    padding: 3rem 0;
  }
  
  section h2 {
    margin-bottom: 2rem;
    text-align: center;
    font-size: 2rem;
  }
  
  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1rem;
  }
  
  /* Match cards */
  .matches-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
    gap: 1.5rem;
  }
  
  .match-card {
    background-color: white;
    border-radius: 10px;
    overflow: hidden;
    box-shadow: 0 4px 15px rgba(0,0,0,0.1);
    padding: 1.5rem;
    display: flex;
    flex-direction: column;
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }
  
  .match-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 10px 25px rgba(0,0,0,0.15);
  }
  
  .match-date {
    font-size: 0.9rem;
    color: #666;
    margin-bottom: 1rem;
  }
  
  .match-teams {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 1.5rem;
  }
  
  .team {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    flex: 1;
  }
  
  .home-team {
    text-align: left;
  }
  
  .away-team {
    text-align: right;
    flex-direction: row-reverse;
  }
  
  .match-vs {
    font-weight: bold;
    color: #999;
    padding: 0 0.5rem;
  }
  
  .match-result {
    font-weight: bold;
    padding: 0.25rem 0.75rem;
    background-color: #f8f9fa;
    border-radius: 4px;
  }
  
  .match-league {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.8rem;
    color: #777;
    margin-bottom: 1rem;
  }
  
  .match-details-link {
    display: inline-block;
    padding: 0.5rem 1rem;
    background-color: #1a2a6c;
    color: white;
    text-decoration: none;
    border-radius: 4px;
    text-align: center;
    margin-top: auto;
    transition: background-color 0.2s;
    font-size: 0.9rem;
  }
  
  .match-details-link:hover {
    background-color: #283b8a;
  }
  
  .result-card {
    border-left: 4px solid #1a2a6c;
  }
  
  /* Features section */
  .features {
    background-color: #f8f9fa;
  }
  
  .features-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 2rem;
  }
  
  .feature-card {
    background: white;
    padding: 2rem;
    border-radius: 10px;
    box-shadow: 0 5px 15px rgba(0,0,0,0.05);
    text-align: center;
    transition: transform 0.3s ease;
  }
  
  .feature-card:hover {
    transform: translateY(-10px);
  }
  
  .feature-icon {
    font-size: 2.5rem;
    margin-bottom: 1rem;
  }
  
  .feature-card h3 {
    margin-bottom: 1rem;
    color: #1a2a6c;
  }
  
  .feature-card p {
    color: #666;
    margin-bottom: 1.5rem;
    line-height: 1.6;
  }
  
  .feature-link {
    color: #1a2a6c;
    text-decoration: none;
    font-weight: bold;
    display: inline-block;
    padding-bottom: 3px;
    border-bottom: 2px solid #1a2a6c;
    transition: all 0.2s;
  }
  
  .feature-link:hover {
    color: #b21f1f;
    border-color: #b21f1f;
  }
  
  /* Utilities */
  .loading-message {
    text-align: center;
    grid-column: 1 / -1;
    padding: 2rem;
    color: #666;
  }
  
  .error-banner {
    background-color: #f8d7da;
    color: #721c24;
    padding: 1rem;
    text-align: center;
    margin: 1rem 0;
    border-radius: 4px;
  }
  
  .view-more {
    text-align: center;
    margin-top: 2rem;
  }
  
  .view-more-link {
    display: inline-block;
    padding: 0.75rem 1.5rem;
    background-color: transparent;
    color: #1a2a6c;
    text-decoration: none;
    border: 2px solid #1a2a6c;
    border-radius: 30px;
    font-weight: bold;
    transition: all 0.3s ease;
  }
  
  .view-more-link:hover {
    background-color: #1a2a6c;
    color: white;
  }
  
  /* Responsive */
  @media (max-width: 768px) {
    .hero h1 {
      font-size: 2.5rem;
    }
    
    .matches-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
